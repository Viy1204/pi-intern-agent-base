# 设计说明

这份文档讲的是「为什么这么做」。想知道「怎么用」看 [README](README.md)。

## 一、这个项目在解决什么问题

公司里会用 AI 的人不少，但能把 AI 接进日常工作流的人很少。差距不在会不会写 prompt，而在于：

- 装环境要过五关（Node、Git、Git Bash、pi、lark-cli），任何一关卡住就放弃了
- 装完之后还是个空壳，要自己找 skill、配飞书、调权限
- 最后就算配好了，也只能在终端里用——而同事们每天待着的地方是飞书

所以这个项目不是「一个 AI 助手」，而是**一整套让非技术同事能真正用上 AI 助手的装配线**：一条命令装好基座（办公 skill + 飞书 skill + 飞书桥接），配好之后在飞书里直接对话，像跟同事聊天一样。

三个刻意的边界：

1. **基座是通用的**，不含任何部门私有内容。HR 的能力在 `pi-intern-hr-pack`，面试官在 `feishu-interview-agent`，按需叠加。这条边界由测试强制（`tests/run-tests.ps1` 里有 HR skill 黑名单断言），不靠自觉。
2. **不存任何人的凭证**。基座里没有 token、appSecret、cookie、个人 open_id、个人路径。每个人自己配自己的。
3. **Windows 是一等公民**，不是「顺带支持」。同事的机器大多是 Windows，所有路径、进程、shell 逻辑都以 Windows 为第一目标平台测试。

## 二、整体架构

```
飞书（同事在这里说话）
   ↓ WebSocket 长连接（不需要公网回调地址）
飞书桥接扩展（extensions/pi-intern-feishu-bridge/）
   ↓ 进程内调用
Pi AgentSession（每个飞书会话一个独立会话）
   ↓
本机文件、脚本、lark-cli、各种 skill
```

### 为什么用 WebSocket 长连接而不是 webhook

webhook 需要一个公网可访问的回调地址——同事的笔记本没有。长连接是飞书官方支持的方案，进程主动连出去，装在自己电脑上就能跑。代价是必须有个常驻进程，于是有了下面的 daemon 设计。

### 为什么飞书连接跑在独立 daemon 里

最初的做法是把飞书连接放在 Pi 的 TUI 进程里，问题立刻暴露：关掉终端窗口，机器人就下线了。而同事的预期是「我给机器人发消息它就该回」，不是「我得先开着一个黑窗口」。

现在的做法：飞书连接跑在一个 detached 的 `pi --mode rpc` 子进程里，日志写文件，TUI 关掉也不影响。命令行的 `/feishu start|stop|restart|status` 只是在管这个 daemon。

这带来两个必须解决的问题——单实例和进程回收：

- **单实例**：同一个飞书应用只能有一条长连接，多开会互相踢。用 `~/.pi/agent/locks.json` 做全局锁（token + pid + 5 秒心跳，30 秒判定失效），抢锁失败的进程自己退出。
- **进程回收**：daemon 是 detached 的，异常退出会留孤儿。这里踩过一个真实的坑：回收函数按命令行特征匹配 daemon 进程，但目录改名后匹配字符串没同步，回收功能整整失效了一段时间没人发现——而且函数在 Windows 上第一行就 `return`，主力平台压根没跑过。现在匹配真实扩展路径，Windows 走 PowerShell 枚举进程。**这件事直接推动了后来加测试和 CI**：这类「看起来在工作但其实是死代码」的问题，只有测试能拦住。

### 会话隔离的粒度

每个飞书会话对应一个独立的 Pi 会话，key 的规则是：

| 场景 | key | 意味着 |
|---|---|---|
| 私聊 | `p2p:<发送人>` | 按人隔离，两个人各聊各的 |
| 话题群的话题 | `group:<群>:thread:<话题>` | 每个话题独立上下文 |
| 普通群 | `group:<群>` | 整个群共享一个上下文 |

普通群共享是有意的——群里大家在讨论同一件事，共享上下文才对。话题独立也是有意的——话题就是「另一件事」。

## 三、安全模型

这部分是后来补的，补之前的状态是：**任何能给机器人发消息的人，都能在你的电脑上以你的身份执行命令**。

### 谁能用（访问控制）

- **owner 自动认领**：配置完成后第一个私聊机器人的人成为 owner，写进 `access.json`。为什么不写在配置文件里让人手填？因为填错 open_id 就把自己锁在外面了，而 open_id 不是人能记住的东西。自动认领的代价是「谁先私聊谁是 owner」，考虑到配置刚做完通常就是本人在测，这个代价可以接受。
- **白名单**：`allowedUsers` / `allowedChats`，owner 之外要放开给谁明确列出来。
- **陌生人静默忽略**：不回复、不加表情、什么都不做。刻意的——回一句「你没有权限」等于确认了这个机器人存在、并且背后有人。

`openAccess: true` 可以退回全放行，给确实需要的场景留了口子。

### 卡片按钮为什么要签名

飞书卡片上的按钮（停止任务、切模型、切历史会话）点击后会回调到本机。原本的实现直接信任回调内容里的 `key` 字段——意味着群里任何人都能点别人的任务卡片，局域网里任何人构造一个 POST 就能停别人的任务。

现在每个按钮的 value 出站时带 HMAC-SHA256 签名（含 7 天过期），回调时 `timingSafeEqual` 验签，同时校验点击者是否在授权名单里。签名密钥自动生成存在本地。

顺带把卡片回调 HTTP 服务的默认绑定从 `0.0.0.0` 改成 `127.0.0.1`——它只需要接收本机转发的请求，没有理由暴露给整个局域网。

### workspace 不是沙箱，但要有护栏

agent 以你的用户身份运行，能读写你能读写的一切。`/workspace` 只是「默认在哪个目录干活」，不是权限边界。

不过「把工作区设成 C:\ 或者主目录」这件事应该拦住——那等于把整台机器的内容都放进 agent 的默认视野。所以 `workspace-policy.ts` 拒绝盘根、主目录及其上级、Desktop/Downloads、系统目录。这拦不住 agent 主动访问外部路径，但能拦住「一句话把整机设成工作区」。

**这里要诚实**：真正的隔离需要用独立的操作系统账号跑 daemon，那是另一个量级的工程。目前的定位是「本机个人助手」，护栏是降低误伤，不是防御恶意。

## 四、可靠性：几个反直觉的设计

这几处都是被真实故障推出来的，不是设计时想到的。

### 超时不能是固定值

原来的实现给每次 agent 调用 180 秒硬超时。看起来合理，实际上：让它去花名册里查个人，读 Excel + 跑 skill 轻松超过三分钟，于是正常任务被杀。

更糟的是超时的实现方式——它只是放弃等待，**没有中止会话**。会话还在后台跑，用户见回复没来就再发一条，撞上还在运行的会话，报一个完全看不懂的 `Agent is already processing`。

现在的做法是**活动感知看门狗**：只要会话还在产生事件（读文件、调工具、输出文本）就不算超时；只有真正静默 5 分钟或总时长超 30 分钟才中止，并且真的调 `abort()`。队列的等待上限也提到大于单任务上限——原来 120 秒就放行，正是双重 prompt 的根源。

**教训**：超时应该衡量「有没有进展」，而不是「花了多久」。

### 连发的消息要合并

飞书里人们习惯把一件事拆成几条短消息发：「帮我查一下」「钟诚的信息」「在花名册里」。逐条起 run 的结果是 agent 看到第一条就开始回答，看到的是半个需求。

`PendingQueue` 的做法：按会话攒消息，静默 600ms 后合并成一轮。run 进行中锁住队列，期间到达的消息累积进下一批。命令（`/stop` 之类）绕过队列，保持即时响应。

### 状态文件要原子写，也要会回收

`writeJson` 原来是直接 `writeFileSync`。进程在写入中途被杀（`taskkill`、关机、崩溃），文件就剩半个 JSON，下次读取解析失败 → 退回默认值 → **整份配置等于消失**。现在先写临时文件 + fsync，再 rename 覆盖：要么旧内容，要么新内容，没有中间态。

回收方面，两张表原本永不清理：`bridge.json` 的投递去重表（每条推送记一行）和 `state.json` 的会话映射（每个飞书会话记一行）。长期运行后文件会到几 MB，而每次读写都是全量解析。现在投递记录 1 天过期，路由 90 天或 500 条上限（保留最近的），会话映射在启动时清掉文件已不存在的条目。

### 消息形态按内容自动选

飞书有三种消息形态，各有各的限制：纯文本（长度限制大但没格式）、富文本 post（有格式但渲染不了表格和代码块）、交互卡片（能渲染 markdown 但单元素约 30KB 上限）。

`rich-text.ts` 按内容特征自动选：有表格/代码块/超长/多标题 → 卡片；多行 markdown → post；否则纯文本。超限时切分，切分点避开半个 emoji（代理对）、优先落在换行处。

这里也有个真实的 bug 是靠测试发现的：表格判定要求分隔行至少三个连字符（`---`），但 GFM 规范允许 `| - | - |`。这类表格被判成 post，而 post 渲染不了表格，用户看到的是一堆竖线。

## 五、面试官项目为什么是「叠加包」而不是合并进来

[feishu-interview-agent](https://github.com/Viy1204/feishu-interview-agent) 让 AI 用真人声音加入飞书视频会议做面试。它没有合并进基座，理由：

1. 基座是公开的通用包，且明确排除部门专属能力——面试官是 HR 专用
2. 一个 TypeScript 一个 Python，生命周期和依赖完全不同
3. 基座已经有成熟的叠加包模式（HR pack 就是这么挂的）

耦合做在三个点上，而不是代码合并：

- **skill 层**：面试官仓库带一个 `feishu-interview` skill，装了基座的人在飞书里说「让面试官进会议 123456789」，Pi 就知道该跑什么
- **凭证层**：面试官的飞书应用凭证可以复用基座桥接的配置（`~/.pi/agent/feishu/config.json`），新同事不用配两遍
- **文档层**：两边 README 互相指向

### 面试官项目自己的两个设计点

**音色和人设是一对一绑定的。** 起初音色（技术参数）和人设（prompt 文本）是两个独立配置，结果很容易配出「清爽磁性男声 + 甜妹台湾腔人设」这种违和组合。现在人设文件头部用 `#voice` / `#name` / `#rate` / `#opening` 指令绑定整套，换任意一端另一端自动跟上。九个人设分别对应九种音色气质，同一套面试纪律（逼数字、逼案例、逼归属）用九种方式表达。

**换供应商靠适配器，不靠改主流程。** 飞书音频那半边（ByteView 双流、采样率转换、打断）和语音模型那半边是解耦的。加一个供应商就是加一个实现相同接口的类（`connect` / `configure_session` / `send_audio` / `listen` / `close`），config 里 `provider` 字段切换。豆包和 Qwen 都是 16k 进 / 24k 出，桥接层零改动。

这个设计在实际调研中救了一次：原计划接 MiniMax，查下去发现它已经没有公开的端到端对话 API 了，换成 Qwen 只是重写一个适配器文件。

## 六、工程约定

### 测试策略

`npm test` = typecheck + 单元测试。另有 `npm run test:pack` 跑打包完整性（skill 清单、AGENTS 模板、HR 黑名单）。CI 三平台矩阵（Windows / macOS / Ubuntu），因为代码里有大量平台分支。

单元测试**只测纯逻辑**，不 mock 飞书 API：会话 key 隔离、消息解析、卡片签名、访问控制、路径护栏、状态回收、消息队列。理由是这些地方最容易出边界 bug，且不需要网络就能测。真实链路靠手动开会议验证。

typecheck 从 13 个错误清到 0 的过程本身发现了三个真问题——其中一个是 pi SDK 根本没在 `package.json` 里声明，npm 靠间接依赖装了个旧版本，而代码写的是新版 API。类型检查在这里的价值不是「让类型更漂亮」，是**暴露了依赖声明的谎言**。

### 提交约定

commit message 说清楚「为什么」，不是「做了什么」——为什么这个改动是必要的、不改会发生什么。这份文档里大部分「为什么」都能在 git log 里找到更详细的版本。

### 已知的欠账

- **日志没有结构化和脱敏**：目前是滚动 JSONL，缺 traceId 传播，也没有对凭据形状字段的自动脱敏。排查跨会话问题时要靠肉眼串日志。
- **卡片没有流式进度**：现在是静态「处理中」卡片 + 结束后一次性回复。长任务期间用户看不到 agent 在干什么。
- **`catch {}` 还有一些**：文件锁、reaction、dispose 这些地方静默吞异常。锁重试耗尽后「无锁执行」的兜底尤其值得改成显式告警。
- **版本号硬编码在多处**：发版要改好几个文件，改漏一个测试才会报。
